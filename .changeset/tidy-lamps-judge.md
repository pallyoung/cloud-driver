---
"@shier-art/cd-cli": patch
---

Ship the first packaged `@shier-art/cd-cli` release flow with bundled web assets, runtime management commands, release automation, and the streamlined explorer UI updates.
